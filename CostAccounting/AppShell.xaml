<?xml version="1.0" encoding="utf-8" ?>
<Shell xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:pages="clr-namespace:CostAccounting" x:DataType="pages:AppShell"
       x:Class="CostAccounting.AppShell">
    <TabBar>
        <Tab Title="Диаграмма" Icon="diagrams.png">
            <ShellContent ContentTemplate="{DataTemplate pages:MainPage}" />
        </Tab>
        <Tab Title="Траты" Icon="spending.png">
            <ShellContent ContentTemplate="{DataTemplate pages:Info}" />
        </Tab>
        <Tab Title="Истории" Icon="history.png">
            <ShellContent ContentTemplate="{DataTemplate pages:Page1}" />
        </Tab>
    </TabBar>
</Shell>