<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="CostAccounting.Info">

    <NavigationPage.TitleView>
        <Label FontSize="24" TextColor="White"  Margin="60,0"/>
    </NavigationPage.TitleView>

    <StackLayout>
        
        
        <ListView x:Name="List" ItemsSource="{Binding Category}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <ViewCell.View>
                            <StackLayout Orientation="Horizontal">
                                <Label Text="{Binding Name}" Margin="13,0,0,0" TextColor="Black" VerticalTextAlignment="Center" FontSize="24" Padding="1"/>
                            </StackLayout>
                        </ViewCell.View>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackLayout>
</ContentPage>